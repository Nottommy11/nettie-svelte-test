async function getAccount(email) {
		try {
			const myres = client.query({
				Method: 'POST',
				query: gql`
					query getAccount {
						net_users_logins {
							email
							password
							username
							passwordrequired
						}
					}
				`
			});

			accounts = (await myres).data;
			console.log(accounts.net_users_logins[1].email)
			return accounts;
		} catch (err) {
			console.log(err);
		}
	}